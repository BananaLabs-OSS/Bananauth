name = "auth-session"
version = "0.2.0"
wasm = "auth-session.wasm"
wasm_sha256 = "68f304e6d60c35038e6ad035ef046e682d21945f9c7ddad371b785d00dedca5c"

provides = [
  "auth.session.v1.create",
  "auth.session.v1.create-owned",
  "auth.session.v1.get",
  "auth.session.v1.revoke",
  "auth.session.v1.revoke-owned",
]
consumes = []
depends_on = []

# This package owns durable session lifecycle only. JWT signing and validation
# remain stateless library behavior in the application compatibility shell.
capabilities = ["storage.sqlite"]
